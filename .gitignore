# Dependencies
# Ignore the node_modules directory, which contains all your project's dependencies.
# These are installed via `npm install` and should not be in version control.
/node_modules

# Build & Packaging
# Ignore directories generated by build tools like Electron Forge or Electron Builder.
/dist
/out
/release
/build
.electron-builder/

# Packaged application files for different operating systems.
*.dmg
*.exe
*.msi
*.p12
*.pfx

# Logs
# Ignore log files that can be generated during development or runtime.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-generated files
# Ignore system files that are created automatically by macOS and Windows.
.DS_Store
Thumbs.db

# Editor Directories
# Ignore configuration files and directories from popular code editors.
# It's good practice to share some settings, so we explicitly un-ignore them.
.vscode/*
!/.vscode/settings.json
!/.vscode/launch.json
!/.vscode/extensions.json
.idea/

# Environment Variables
# Ignore files containing sensitive information like API keys or secrets.
.env
.env.local
.env.*.local